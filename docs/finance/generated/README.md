# Generated financial reports

Read `authoritative_manifest.json`, then open files only from its `run_path`.

Files directly in this directory predate versioned publication. They are preserved as non-authoritative legacy outputs and must not be used for decisions. A complete current generation lives under `runs/<generation_id>/` and becomes authoritative only through the atomic manifest pointer.
