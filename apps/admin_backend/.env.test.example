# Local test environment template only.
# These values are fake defaults that match the local/CI test container contract.
# Do not put real secrets in this file.

NODE_ENV=test
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=admin
DB_DATABASE=omnifood
JWT_SECRET=test-secret
